UPDATE CWTITULO SET CONTA = 
(
SELECT CONTA FROM CWMOVIME WHERE  CWTITULO.TITULO = CWMOVIME.TITULO AND CWMOVIME.DATAHORA = 
   (SELECT MIN(DATAHORA) FROM CWMOVIME WHERE CWTITULO.TITULO = CWMOVIME.TITULO)
)
WHERE (CONTA is null) and (situacao = 1 or situacao = 2)